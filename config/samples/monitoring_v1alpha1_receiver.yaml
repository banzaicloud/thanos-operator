apiVersion: monitoring.banzaicloud.io/v1alpha1
kind: Receiver
metadata:
  name: receiver-sample
spec:
  receiverGroups:
    - groupName: receiver1
      config:
        mountFrom:
          secretKeyRef:
            name: thanos
            key: object-store.yaml
      dataVolume:
        pvc:
          spec:
            accessModes:
            - ReadWriteOnce
            resources:
              requests:
                storage: 1Gi
      metrics:
        serviceMonitor: true
      httpIngress: {}
