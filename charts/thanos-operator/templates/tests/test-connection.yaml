apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanos-operator.fullname" . }}-test-connection"
  labels:
    {{- include "thanos-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanos-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
